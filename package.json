{
  "name": "graphql-api-project",
  "engines": {
    "node": "20.x"
  },
  "version": "1.0.0",
  "scripts": {
    "install": "cd backend && rm -rf node_modules package-lock.json && npm install --build-from-source=sqlite3 && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start"
  }
}
